# Expo / React Native dependency graphs frequently trip npm's strict peer-dependency
# resolver during SDK upgrades (e.g. expo-router's internal @expo/* peers). Using
# legacy peer resolution avoids spurious ERESOLVE failures. EAS build honors this too.
legacy-peer-deps=true
